# torch installed separately: pip install torch --index-url https://download.pytorch.org/whl/cu128
# (default PyPI torch is built for CUDA 13.0; this machine's driver is CUDA 12.8)
torch
transformers>=4.51.0
datasets
accelerate
pandas
tqdm
ipykernel
python-dotenv
matplotlib
openai
